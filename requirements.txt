# Requirements for enhanced GUI and iOS support (branch: feature/full-refactor-ui-ios-support)
# PySide6 is used for the new cross-platform GUI.
# libimobiledevice is a native tool (not pip). See README.IOS.md for installation instructions.

PySide6>=6.5.0
# Optional utilities
psutil>=5.9.0

# Notes:
# - libimobiledevice is not available via pip; install platform binaries as documented in README.IOS.md
# - Keep original tkinter-based code if you prefer the lightweight script version
